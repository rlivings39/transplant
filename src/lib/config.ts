/**
 * Feature flags and configuration for Transplant
 * Use this to control which features are enabled/disabled
 */
export const FEATURES = {
  csvMapper: true,
  oldTables: false  // Disable old table features when working on new ones
};
